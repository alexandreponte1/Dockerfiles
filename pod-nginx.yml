apiVersion: v1
kind: Pod
metadata:
  name: goku-nginx
  labels:
    app: label-pod-goku-nginx
spec:
  containers:
    - name: container-pod-nginx
      image: nginx:latest
      ports:
        - containerPort: 80